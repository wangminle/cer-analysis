[[source]]
url = "https://pypi.tuna.tsinghua.edu.cn/simple"
verify_ssl = true
name = "pypi"

[packages]
# V2 核心依赖（已移除 jiwer + pandas）
jieba = ">=0.42.1"
python-levenshtein = ">=0.12.2"

[dev-packages]
pytest = ">=6.0"
pytest-cov = "*"
ruff = "*"
mypy = "*"
# 可选分词器（仅开发环境）
thulac = ">=0.2.0"
hanlp = ">=2.1.0"
torch = "*"

[requires]
python_version = "3.12"
